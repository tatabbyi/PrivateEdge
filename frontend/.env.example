# Where Vite proxies /api and /ws (dev + preview). Use loopback; daphne can still bind 0.0.0.0:8000.
PRIVATEEDGE_API_ORIGIN=http://127.0.0.1:8000

# Optional: only if UI and API are on different origins in production builds.
# VITE_API_BASE=
# VITE_WS_URL=
